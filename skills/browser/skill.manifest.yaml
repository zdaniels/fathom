name: browser
version: "1.0.0"
author: fathom-team
signature: ed25519:builtin
description: Browse the web, navigate pages, take screenshots and extract content using headless browser
category: web
tags:
  - browser
  - scraping
  - playwright
  - web
permissions:
  network:
    - GET https://*/*
    - POST https://*/*
  filesystem: read-write
  shell: none
  memory: own
